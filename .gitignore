
!.env.example
# Build output
# Dependencies (installed by npm/Netlify, never tracked)
# Editor / OS
# Environment / secrets — never commit
# Local Netlify folder
.DS_Store
.env
.env.*
.idea
.netlify
deno.lock
dist
node_modules/
